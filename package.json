{
  "name": "adonay",
  "version": "1.0.0",
  "description": "Sistema de inventario y pedidos — Adonay",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check-views": "node scripts/check-views-sync.js",
    "test:rules": "firebase emulators:exec --only database \"node --test rules-emulator/database-rules.js\"",
    "emulators": "firebase emulators:start --only database"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase-tools": "^13.0.0",
    "jsdom": "^24.1.3"
  },
  "license": "UNLICENSED"
}
